# Secrets live in .env; SearXNG settings.yml is safe to version when
# server.secret_key is provided through SEARXNG_SECRET.
searxng/*.db
searxng/*.sqlite*

# Python
__pycache__/
*.py[cod]
.venv/

# Node/editor
node_modules/
.DS_Store
.env
searxng/secrets/
